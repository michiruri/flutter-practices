name: capitis_mad2_assignment_4
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.8.0-70.0.dev

dependencies:
  audioplayers: ^6.2.0
  flutter:
    sdk: flutter
  image_color_builder: ^1.0.4

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/audio/
    - assets/images/
